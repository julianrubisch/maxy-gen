require "spec_helper"

RSpec.describe Maxy::Gen do
  it "has a version number" do
    expect(Maxy::Gen::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
